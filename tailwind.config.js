/** @type {import('tailwindcss').Config} */
module.exports = {
  /** Tailwind will check these files, if styles aren't used in a new extension,
      make sure to add them here. */
  content: ['./src/**/*.html', './src/**/*.njk'],
  theme: {
    extend: {},
  },
  plugins: [],
};
